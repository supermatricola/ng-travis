language: node_js
node_js:
  - 8.12.0

branches:
  only:
    - master # will only build for master branch commits

install:
  - rm -rf dist/*
  dist: trusty
sudo: false

language: node_js
node_js:
  - 9

branches:
  only:
    - master # will only build for master branch commits

install:
  - rm -rf dist/*

script:
  - npm install -g --silent @angular/cli
  - ng build --prod
  - ng e2e
  - ng test 

after_success:
  - echo "OK!"


before_script:
  x

script:
  - ng build --prod
  - ng e2e
  - ng test 

after_success:
  - echo "OK!"
